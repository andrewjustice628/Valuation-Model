# Copy this file to `.env` and fill in your key. `.env` is gitignored — never
# commit real keys. In production, set the same variable in the Netlify UI
# (Site settings → Environment variables), NOT in the repo.

# Finnhub API key — used only server-side by netlify/functions/quote.ts.
FINNHUB_API_KEY=
